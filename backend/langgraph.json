{
  "node_version": "20",
  "graphs": {
    "agent": "./src/graph.ts:chatGraph"
  },
  "env": ".env",
  "dependencies": ["."],
  "store": {
    "provider": "postgres",
    "conn_string": "${DATABASE_URL}"
  },
  "checkpointer": {
    "provider": "postgres",
    "conn_string": "${DATABASE_URL}"
  }
}
